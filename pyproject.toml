[project]
name = "simple-iotest"
version = "3.53"
description = "A dead simple iotest for HPC environments"
readme = "README.md"
requires-python = ">=3.6"
dependencies = [
    "argparse",
]

[project.scripts]
iotest = "iotest:climain"